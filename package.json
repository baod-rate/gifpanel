{
  "name": "GifPanel-for-GitHub",
  "version": "0.0.1",
  "description": "Search and insert GIFs in GitHub comments",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
